using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicSceneManager : MonoBehaviour
{

    /**
     * Replacement for Older SceneManager
     * 
     * Hurricane world is separated into 'Scenes' with set events/animations, narration, on-screen UI text, and controller functionality.
     * 
     *  
     * 
     * */

    public class Scene
    {

    }


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
